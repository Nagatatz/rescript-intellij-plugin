# Tasklist: パンくずナビゲーション

## タスク

- [x] 1. `RescriptPsiUtils.kt` を作成し、`extractName()` / `getIcon()` / `getElementDescription()` / `NAVIGABLE_TYPES` を実装する
- [x] 2. `RescriptStructureViewElement.kt` を `RescriptPsiUtils` に委譲するようリファクタリングする
- [x] 3. `RescriptBreadcrumbsProvider.kt` を作成し、`BreadcrumbsProvider` を実装する
- [x] 4. `plugin.xml` に `breadcrumbsInfoProvider` を登録する
- [x] 5. ビルド確認（`./gradlew buildPlugin`）
- [x] 6. コミット
