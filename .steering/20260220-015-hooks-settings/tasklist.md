# Tasklist: Hooks + settings.json

## 実装タスク

- [x] `.claude/hooks/` ディレクトリを作成する
- [x] `.claude/settings.json` を作成する（permissions, env, hooks 定義）
- [x] `.claude/hooks/validate-bash.sh` を作成する（Bash コマンド検証）
- [x] `.claude/hooks/validate-file-edit.sh` を作成する（ファイル編集検証）
- [x] スクリプトに実行権限を付与する（`chmod +x`）

## テストタスク

- テスト省略理由: Claude Code の設定ファイルとシェルスクリプトであり、Kotlin コードの変更を伴わないため、ユニットテストの対象外

## 完了タスク

- [x] tasklist.md を更新してコミットする
- [ ] ユーザーにマージ確認を行う
