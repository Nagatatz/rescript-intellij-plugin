// Verification sample for `didYouMean` LSP code action.
// Place caret on `myValu` and trigger Alt+Enter.
// Expected: quick fix replacing the typo with the suggested `myValue`.
let myValue = 42

let _ = myValu
