# Tasklist: フォーマッタ連携

## タスク

- [x] `src/main/kotlin/com/rescript/plugin/formatter/RescriptFormattingService.kt` を作成
- [x] `plugin.xml` に `formattingService` を登録
- [x] ビルド確認（`./gradlew buildPlugin`）
- [x] `CLAUDE.md` のプロジェクト構成に formatter パッケージを追記
- [x] コミット: `✨ Add external formatter integration with rescript format CLI`
