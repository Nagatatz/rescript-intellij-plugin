# Tasklist: Surround With

## 実装タスク

- [x] `RescriptSurroundDescriptor.kt` を作成（Descriptor + 4 Surrounder クラス）
- [x] `plugin.xml` に `<lang.surroundDescriptor>` を登録
- [x] `RescriptSurroundDescriptorTest.kt` を作成
- [x] ビルド確認（`./gradlew buildPlugin`）
- [x] コミット: `✨ Add Surround With support`
