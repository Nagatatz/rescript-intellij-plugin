# Requirements: //#region 折りたたみ

## 概要
`//#region` ... `//#endregion` コメントによるカスタム折りたたみリージョンのサポート。

## 機能要件
- `//#region [名前]` と `//#endregion` でカスタム折りたたみを定義可能
- `// #region` (スペース付き) 形式もサポート
- 折りたたみ時にリージョン名を表示
- 名前なしの場合は `...` を表示
- 既存の折りたたみ機能（ブロックコメント、モジュール、JSX）に影響なし
