# タスクリスト: サンプル package.json バージョンバンプ

## セクション A: 編集 + 検証

- [x] `manual-test-projects/main/package.json` で `rescript` を `^12.2.0` に
- [x] 同 `@rescript/language-server` を `^1.72.0` に
- [x] `./gradlew clean buildPlugin test` グリーン
- [x] `🔧 Bump manual test sample ReScript to 12.2 and language-server to 1.72` でコミット

## セクション B: マージ

- [x] tasklist の全項目を `[x]` に更新（このコミットに含める）
- [x] `main` にマージ、worktree クリーンアップ
