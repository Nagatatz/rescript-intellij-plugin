---
description: /clear 後にセッション状態を復元する。コンパクション後のコンテキスト回復にも使用可能。
allowed-tools: Read, Glob, Bash
disable-model-invocation: true
---

`/clear` や `/compact` 後にセッション状態を復元し、作業の継続に必要なコンテキストを再構築する。

まず `INSTRUCTIONS.md` を Read ツールで読み込み、その手順に従うこと。
