name: Docs

on:
  push:
    branches: [main]
    paths: ['sphinx-docs/**']
  pull_request:
    branches: [main]
    paths: ['sphinx-docs/**']

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sphinx-docs

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Mypy type check
        run: uv run mypy _ext/generate_rescript_lexer.py

      - name: Pytest
        run: uv run pytest

  build:
    needs: lint-and-test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sphinx-docs

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate ReScript Pygments lexer
        run: make generate-lexer

      - name: Build English HTML
        run: make html

      - name: Build Japanese HTML
        run: make build-ja

      - name: Assemble site
        run: |
          rm -rf _build/site
          mkdir -p _build/site
          cp -r _build/html _build/site/en
          cp -r _build/html_ja _build/site/ja
          echo '<!DOCTYPE html><html><head><meta http-equiv="refresh" content="0;url=en/"></head><body><a href="en/">English</a></body></html>' > _build/site/index.html

      - name: Build Pagefind search index
        run: npx pagefind --site _build/site

      - name: Upload Pages artifact
        if: github.event_name == 'push'
        uses: actions/upload-pages-artifact@v4
        with:
          path: sphinx-docs/_build/site

      - name: Link check (PR only)
        if: github.event_name == 'pull_request'
        run: make linkcheck
        continue-on-error: true

  deploy:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
